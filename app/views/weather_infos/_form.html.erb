<%= form_for(@weather_info) do |f| %>
  <% if @weather_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weather_info.errors.count, "error") %> prohibited this weather_info from being saved:</h2>

      <ul>
      <% @weather_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :temperature %><br>
    <%= f.text_field :temperature %>
  </div>
  <div class="field">
    <%= f.label :humidity %><br>
    <%= f.text_field :humidity %>
  </div>
  <div class="field">
    <%= f.label :generated_at %><br>
    <%= f.text_field :generated_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
