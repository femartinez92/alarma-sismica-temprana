<p id="notice"><%= notice %></p>

<h1>Listing Weather Infos</h1>

<table>
  <thead>
    <tr>
      <th>Temperature</th>
      <th>Humidity</th>
      <th>Generated at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @weather_infos.each do |weather_info| %>
      <tr>
        <td><%= weather_info.temperature %></td>
        <td><%= weather_info.humidity %></td>
        <td><%= weather_info.generated_at %></td>
        <td><%= link_to 'Show', weather_info %></td>
        <td><%= link_to 'Edit', edit_weather_info_path(weather_info) %></td>
        <td><%= link_to 'Destroy', weather_info, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Weather info', new_weather_info_path %>
